플러터 다시 설치 하는 법

1.개인방화벽 끄기
2. 환경변수 path에 들어가서 플러터 삭제하기
3. cmd 열기
4. cmd에 플러터 있는지 flutter --version 쳐서 확인
5. 없으면 Powershell에서 # Flutter SDK 설치 자동화 스크립트
6. $flutterPath = "$env:USERPROFILE\flutter"
  git clone https://github.com/flutter/flutter.git -b stable $flutterPath

  # 환경 변수에 flutter/bin 추가
  setx PATH "$env:PATH;$flutterPath\bin"

  # 설치 확인(새로운 터미널로 열어서 확인해야 함)
  flutter doctor  입력하기
7. 새 Powershell에서 flutter doctor 입력하기
8. cd project_c 입력하기
9. flutter create second_app 입력하기
10. cd second_app 입력하기
11. flutter run 입력하기
12. 2 입력하기
